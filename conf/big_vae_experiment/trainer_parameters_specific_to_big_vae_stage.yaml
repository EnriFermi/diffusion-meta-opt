max_steps: 10000
grad_accum_steps: 4

warmup_steps: 500
min_lr_ratio: 0.01
kl_beta: 0.05

behavioral_coef: 0.1
structural_coef: 2.0
contrastive_coef: 0.0

checkpoint_every: 500
checkpoint_dir: ${training_artifacts.big_vae_checkpoint_dir}
