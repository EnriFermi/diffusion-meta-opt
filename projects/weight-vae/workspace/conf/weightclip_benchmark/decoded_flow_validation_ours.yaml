schema_version: 2
template_only: true
codec: ours
resolution_contract: training.weightclip_benchmark.resolve_stage_dg_dag
note: Per-arm checkpoint, normalizer, exact validation records/bundles, AE seal and output are emitted only in the resolved DAG.
